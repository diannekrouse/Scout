# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# local lifecycle overlay (optional write surface)
/data/lifecycle.db
/data/lifecycle.db-*

# substrate overlay files written at runtime (live in DOSSIER_ROOT, never modify
# the canonical index/*.json). These are user state, not source code.
sample-dossier/bundles/
sample-dossier/library-card.json
sample-dossier/curation.json

# editor
.vscode/
.idea/

# Privacy guard — local-only sensitive denylist (never commit)
.privacy-denylist
privacy-audit.log
